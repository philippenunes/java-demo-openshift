apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: my-java-app
  labels:
    app: my-java-app
spec:
  to:
    kind: Service
    name: my-java-app
  port:
    targetPort: 8080
  tls:
    termination: edge