# 🏗️ BUILD RESOURCES - OPENSHIFT S2I
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: my-java-app
  labels:
    app: my-java-app
spec:
  source:
    type: Git
    git:
      uri: https://github.com/philippenunes/java-demo-openshift
      ref: main
    contextDir: .
  strategy:
    type: Source
    sourceStrategy:
      from:
        kind: ImageStreamTag
        namespace: openshift
        name: java:openjdk-17-ubi8
      incremental: true
  output:
    to:
      kind: ImageStreamTag
      name: my-java-app:latest
  triggers:
    - type: GitHub
      github:
        secret: my-java-app-github-webhook
    - type: Generic
      generic:
        secret: my-java-app-generic-webhook
    - type: ImageChange
      imageChange: {}
    - type: ConfigChange
  runPolicy: Serial