# 🔐 VARIÁVEIS DE AMBIENTE - TEMPLATE
# Copie este arquivo para .env e preencha com seus valores reais

# GitHub Token para ArgoCD Image Updater
# Permissões necessárias: repo, write:packages
GITHUB_TOKEN=ghp_YOUR_GITHUB_TOKEN_HERE

# Registry interno OpenShift (geralmente não precisa alterar)
OPENSHIFT_REGISTRY=image-registry.openshift-image-registry.svc:5000

# Namespace do projeto
PROJECT_NAMESPACE=philippenunes-dev

# ArgoCD Admin Password (se customizado)
ARGOCD_ADMIN_PASSWORD=your-custom-admin-password

# 📝 INSTRUÇÕES DE USO:
# 1. Copie: cp .env.example .env
# 2. Edite o arquivo .env com seus valores reais
# 3. Para aplicar secrets: source scripts/apply-secrets.sh
# 4. NUNCA commite o arquivo .env (já está no .gitignore)